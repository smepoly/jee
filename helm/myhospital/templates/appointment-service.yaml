apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: appointment-mysql
  namespace: {{ .Values.namespace }}
spec:
  serviceName: "appointment-mysql"
  replicas: 1
  selector:
    matchLabels:
      app: appointment-mysql
  template:
    metadata:
      labels:
        app: appointment-mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: mysql-root-password
        - name: MYSQL_DATABASE
          value: "{{ .Values.appointmentService.database.name }}"
        resources:
          requests:
            memory: "{{ .Values.appointmentService.database.resources.requests.memory }}"
            cpu: "{{ .Values.appointmentService.database.resources.requests.cpu }}"
          limits:
            memory: "{{ .Values.appointmentService.database.resources.limits.memory }}"
            cpu: "{{ .Values.appointmentService.database.resources.limits.cpu }}"
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mysql-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: {{ .Values.appointmentService.database.storage }}
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-mysql
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: appointment-mysql
  ports:
  - port: 3306
    targetPort: 3306
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.appointmentService.replicas }}
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      containers:
      - name: appointment-service
        image: "{{ .Values.dockerNamespace }}/appointment-service:{{ .Values.imageTag }}"
        ports:
        - containerPort: 8087
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://appointment-mysql:3306/{{ .Values.appointmentService.database.name }}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC"
        - name: SPRING_DATASOURCE_USERNAME
          value: "root"
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: mysql-root-password
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: eureka-config
              key: EUREKA_SERVER_URL
        resources:
          requests:
            memory: "{{ .Values.appointmentService.resources.requests.memory }}"
            cpu: "{{ .Values.appointmentService.resources.requests.cpu }}"
          limits:
            memory: "{{ .Values.appointmentService.resources.limits.memory }}"
            cpu: "{{ .Values.appointmentService.resources.limits.cpu }}"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8087
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8087
          initialDelaySeconds: 90
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: appointment-service
  ports:
  - port: 8087
    targetPort: 8087
  type: ClusterIP
