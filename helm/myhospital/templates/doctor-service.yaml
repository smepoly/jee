apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: doctor-mysql
  namespace: {{ .Values.namespace }}
spec:
  serviceName: "doctor-mysql"
  replicas: 1
  selector:
    matchLabels:
      app: doctor-mysql
  template:
    metadata:
      labels:
        app: doctor-mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: mysql-root-password
        - name: MYSQL_DATABASE
          value: "{{ .Values.doctorService.database.name }}"
        resources:
          requests:
            memory: "{{ .Values.doctorService.database.resources.requests.memory }}"
            cpu: "{{ .Values.doctorService.database.resources.requests.cpu }}"
          limits:
            memory: "{{ .Values.doctorService.database.resources.limits.memory }}"
            cpu: "{{ .Values.doctorService.database.resources.limits.cpu }}"
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mysql-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: {{ .Values.doctorService.database.storage }}
---
apiVersion: v1
kind: Service
metadata:
  name: doctor-mysql
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: doctor-mysql
  ports:
  - port: 3306
    targetPort: 3306
  clusterIP: None
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctor-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.doctorService.replicas }}
  selector:
    matchLabels:
      app: doctor-service
  template:
    metadata:
      labels:
        app: doctor-service
    spec:
      containers:
      - name: doctor-service
        image: "{{ .Values.dockerNamespace }}/doctor-service:{{ .Values.imageTag }}"
        ports:
        - containerPort: 8086
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://doctor-mysql:3306/{{ .Values.doctorService.database.name }}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC"
        - name: SPRING_DATASOURCE_USERNAME
          value: "root"
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: mysql-root-password
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: eureka-config
              key: EUREKA_SERVER_URL
        resources:
          requests:
            memory: "{{ .Values.doctorService.resources.requests.memory }}"
            cpu: "{{ .Values.doctorService.resources.requests.cpu }}"
          limits:
            memory: "{{ .Values.doctorService.resources.limits.memory }}"
            cpu: "{{ .Values.doctorService.resources.limits.cpu }}"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8086
          initialDelaySeconds: 90
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8086
          initialDelaySeconds: 60
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: doctor-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: doctor-service
  ports:
  - port: 8086
    targetPort: 8086
  type: ClusterIP
