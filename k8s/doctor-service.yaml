# Doctor MySQL Database
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: doctor-mysql
  namespace: myhospital
spec:
  serviceName: doctor-mysql
  replicas: 1
  selector:
    matchLabels:
      app: doctor-mysql
  template:
    metadata:
      labels:
        app: doctor-mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_ROOT_PASSWORD
        - name: MYSQL_DATABASE
          value: "doctordb"
        volumeMounts:
        - name: doctor-mysql-storage
          mountPath: /var/lib/mysql
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
  volumeClaimTemplates:
  - metadata:
      name: doctor-mysql-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: doctor-mysql
  namespace: myhospital
spec:
  clusterIP: None
  selector:
    app: doctor-mysql
  ports:
  - port: 3306
    targetPort: 3306
---
# Doctor Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctor-service
  namespace: myhospital
  labels:
    app: doctor-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: doctor-service
  template:
    metadata:
      labels:
        app: doctor-service
    spec:
      containers:
      - name: doctor-service
        image: sabri74155/doctor-service:latest
        ports:
        - containerPort: 8086
        env:
        - name: SPRING_APPLICATION_NAME
          value: "doctor-service"
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: eureka-config
              key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://doctor-mysql:3306/doctordb?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC"
        - name: SPRING_DATASOURCE_USERNAME
          value: "root"
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_ROOT_PASSWORD
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8086
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8086
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: doctor-service
  namespace: myhospital
spec:
  type: ClusterIP
  selector:
    app: doctor-service
  ports:
  - port: 8086
    targetPort: 8086
    protocol: TCP
