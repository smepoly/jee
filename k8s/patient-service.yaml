# Patient MySQL Database
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: patient-mysql
  namespace: myhospital
spec:
  serviceName: patient-mysql
  replicas: 1
  selector:
    matchLabels:
      app: patient-mysql
  template:
    metadata:
      labels:
        app: patient-mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_ROOT_PASSWORD
        - name: MYSQL_DATABASE
          value: "patientdb"
        volumeMounts:
        - name: patient-mysql-storage
          mountPath: /var/lib/mysql
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
  volumeClaimTemplates:
  - metadata:
      name: patient-mysql-storage
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: patient-mysql
  namespace: myhospital
spec:
  clusterIP: None
  selector:
    app: patient-mysql
  ports:
  - port: 3306
    targetPort: 3306
---
# Patient Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: patient-service
  namespace: myhospital
  labels:
    app: patient-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: patient-service
  template:
    metadata:
      labels:
        app: patient-service
    spec:
      containers:
      - name: patient-service
        image: sabri74155/patient-service:latest
        ports:
        - containerPort: 8085
        env:
        - name: SPRING_APPLICATION_NAME
          value: "patient-service"
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: eureka-config
              key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://patient-mysql:3306/patientdb?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC"
        - name: SPRING_DATASOURCE_USERNAME
          value: "root"
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_ROOT_PASSWORD
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8085
          initialDelaySeconds: 30
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: patient-service
  namespace: myhospital
spec:
  type: ClusterIP
  selector:
    app: patient-service
  ports:
  - port: 8085
    targetPort: 8085
    protocol: TCP
